/*
 *  Rule contains a single rule: the input, the output, and the delete button
 */

import React from "react";

const Rule = props => {
	return <div>
        <input/>
        <input/>
        <button>x</button>
    </div>;
};

export default Rule;
